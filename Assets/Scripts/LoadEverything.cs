using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadEverything : MonoBehaviour {

	// Use this for initialization
	void Awake () {
		//transform.position = new Vector3(SceneParameters.imagePosition.x,SceneParameters.imagePosition.y,SceneParameters.imagePosition.z);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
